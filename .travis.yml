language: go

services:
  - docker

go:
  - 1.9

before_script:
after_script:
script:

jobs:
 include:
  - stage: coverage
    script: make coverage
  - stage: benchmarks
    script: make benchmarks
  - stage: test
    script: make tests-travis
